<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="326" onload="init(evt)" viewBox="0 0 1200 326" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="326" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">Flame Graph</text><text id="details" fill="rgb(0,0,0)" x="10" y="309.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="309.00"> </text><svg id="frames" x="10" width="1180" total_samples="29163"><g><title>libsystem_kernel.dylib`madvise (303 samples, 1.04%)</title><rect x="1.2139%" y="101" width="1.0390%" height="15" fill="rgb(227,0,7)" fg:x="354" fg:w="303"/><text x="1.4639%" y="111.50"></text></g><g><title>libsystem_malloc.dylib`medium_free_scan_madvise_free (31 samples, 0.11%)</title><rect x="2.2597%" y="101" width="0.1063%" height="15" fill="rgb(217,0,24)" fg:x="659" fg:w="31"/><text x="2.5097%" y="111.50"></text></g><g><title>libsystem_kernel.dylib`madvise (31 samples, 0.11%)</title><rect x="2.2597%" y="85" width="0.1063%" height="15" fill="rgb(221,193,54)" fg:x="659" fg:w="31"/><text x="2.5097%" y="95.50"></text></g><g><title>libsystem_malloc.dylib`medium_madvise_free_range_no_lock (50 samples, 0.17%)</title><rect x="2.3660%" y="101" width="0.1715%" height="15" fill="rgb(248,212,6)" fg:x="690" fg:w="50"/><text x="2.6160%" y="111.50"></text></g><g><title>libsystem_kernel.dylib`madvise (50 samples, 0.17%)</title><rect x="2.3660%" y="85" width="0.1715%" height="15" fill="rgb(208,68,35)" fg:x="690" fg:w="50"/><text x="2.6160%" y="95.50"></text></g><g><title>libsystem_malloc.dylib`free_medium (387 samples, 1.33%)</title><rect x="1.2139%" y="117" width="1.3270%" height="15" fill="rgb(232,128,0)" fg:x="354" fg:w="387"/><text x="1.4639%" y="127.50"></text></g><g><title>libsystem_malloc.dylib`mvm_deallocate_pages (14 samples, 0.05%)</title><rect x="2.5409%" y="117" width="0.0480%" height="15" fill="rgb(207,160,47)" fg:x="741" fg:w="14"/><text x="2.7909%" y="127.50"></text></g><g><title>libsystem_kernel.dylib`_kernelrpc_mach_vm_deallocate_trap (14 samples, 0.05%)</title><rect x="2.5409%" y="101" width="0.0480%" height="15" fill="rgb(228,23,34)" fg:x="741" fg:w="14"/><text x="2.7909%" y="111.50"></text></g><g><title>progset1`&lt;hashbrown::raw::RawTable&lt;T,A&gt; as core::ops::drop::Drop&gt;::drop (751 samples, 2.58%)</title><rect x="0.0171%" y="133" width="2.5752%" height="15" fill="rgb(218,30,26)" fg:x="5" fg:w="751"/><text x="0.2671%" y="143.50">pr..</text></g><g><title>progset1`&lt;progset1::graph_gen::Vertex4D as progset1::graph_gen::VertexCoord&lt;progset1::graph_gen::Vertex4D&gt;&gt;::dist (497 samples, 1.70%)</title><rect x="2.5923%" y="133" width="1.7042%" height="15" fill="rgb(220,122,19)" fg:x="756" fg:w="497"/><text x="2.8423%" y="143.50"></text></g><g><title>progset1`core::hash::BuildHasher::hash_one (2,312 samples, 7.93%)</title><rect x="4.2965%" y="133" width="7.9279%" height="15" fill="rgb(250,228,42)" fg:x="1253" fg:w="2312"/><text x="4.5465%" y="143.50">progset1`co..</text></g><g><title>progset1`core::hash::Hasher::write_i64 (5,149 samples, 17.66%)</title><rect x="12.2244%" y="133" width="17.6559%" height="15" fill="rgb(240,193,28)" fg:x="3565" fg:w="5149"/><text x="12.4744%" y="143.50">progset1`core::hash::Hasher..</text></g><g><title>progset1`hashbrown::map::HashMap&lt;K,V,S,A&gt;::get_mut (6 samples, 0.02%)</title><rect x="29.8803%" y="133" width="0.0206%" height="15" fill="rgb(216,20,37)" fg:x="8714" fg:w="6"/><text x="30.1303%" y="143.50"></text></g><g><title>libsystem_malloc.dylib`szone_malloc_should_clear (4 samples, 0.01%)</title><rect x="41.5115%" y="37" width="0.0137%" height="15" fill="rgb(206,188,39)" fg:x="12106" fg:w="4"/><text x="41.7615%" y="47.50"></text></g><g><title>libsystem_malloc.dylib`szone_realloc (14 samples, 0.05%)</title><rect x="41.4841%" y="53" width="0.0480%" height="15" fill="rgb(217,207,13)" fg:x="12098" fg:w="14"/><text x="41.7341%" y="63.50"></text></g><g><title>libsystem_malloc.dylib`malloc_zone_realloc (230 samples, 0.79%)</title><rect x="41.4704%" y="69" width="0.7887%" height="15" fill="rgb(231,73,38)" fg:x="12094" fg:w="230"/><text x="41.7204%" y="79.50"></text></g><g><title>libsystem_platform.dylib`_platform_memmove (212 samples, 0.73%)</title><rect x="41.5321%" y="53" width="0.7269%" height="15" fill="rgb(225,20,46)" fg:x="12112" fg:w="212"/><text x="41.7821%" y="63.50"></text></g><g><title>libsystem_malloc.dylib`small_size (3 samples, 0.01%)</title><rect x="42.2625%" y="69" width="0.0103%" height="15" fill="rgb(210,31,41)" fg:x="12325" fg:w="3"/><text x="42.5125%" y="79.50"></text></g><g><title>libsystem_malloc.dylib`realloc (240 samples, 0.82%)</title><rect x="41.4601%" y="85" width="0.8230%" height="15" fill="rgb(221,200,47)" fg:x="12091" fg:w="240"/><text x="41.7101%" y="95.50"></text></g><g><title>libsystem_malloc.dylib`szone_size (3 samples, 0.01%)</title><rect x="42.2727%" y="69" width="0.0103%" height="15" fill="rgb(226,26,5)" fg:x="12328" fg:w="3"/><text x="42.5227%" y="79.50"></text></g><g><title>libsystem_malloc.dylib`tiny_size (3 samples, 0.01%)</title><rect x="42.2727%" y="53" width="0.0103%" height="15" fill="rgb(249,33,26)" fg:x="12328" fg:w="3"/><text x="42.5227%" y="63.50"></text></g><g><title>progset1`alloc::raw_vec::RawVec&lt;T,A&gt;::reserve_for_push (246 samples, 0.84%)</title><rect x="41.4464%" y="117" width="0.8435%" height="15" fill="rgb(235,183,28)" fg:x="12087" fg:w="246"/><text x="41.6964%" y="127.50"></text></g><g><title>progset1`alloc::raw_vec::finish_grow (244 samples, 0.84%)</title><rect x="41.4532%" y="101" width="0.8367%" height="15" fill="rgb(221,5,38)" fg:x="12089" fg:w="244"/><text x="41.7032%" y="111.50"></text></g><g><title>progset1`core::hash::BuildHasher::hash_one (1,537 samples, 5.27%)</title><rect x="42.2899%" y="117" width="5.2704%" height="15" fill="rgb(247,18,42)" fg:x="12333" fg:w="1537"/><text x="42.5399%" y="127.50">progse..</text></g><g><title>progset1`core::hash::Hasher::write_i64 (5,526 samples, 18.95%)</title><rect x="47.5603%" y="117" width="18.9487%" height="15" fill="rgb(241,131,45)" fg:x="13870" fg:w="5526"/><text x="47.8103%" y="127.50">progset1`core::hash::Hasher::w..</text></g><g><title>libsystem_malloc.dylib`free_medium (6 samples, 0.02%)</title><rect x="66.5261%" y="85" width="0.0206%" height="15" fill="rgb(249,31,29)" fg:x="19401" fg:w="6"/><text x="66.7761%" y="95.50"></text></g><g><title>libsystem_kernel.dylib`madvise (6 samples, 0.02%)</title><rect x="66.5261%" y="69" width="0.0206%" height="15" fill="rgb(225,111,53)" fg:x="19401" fg:w="6"/><text x="66.7761%" y="79.50"></text></g><g><title>progset1`progset1::graph_gen::CompleteUnitGraph::graph_nd (10,690 samples, 36.66%)</title><rect x="29.9009%" y="133" width="36.6560%" height="15" fill="rgb(238,160,17)" fg:x="8720" fg:w="10690"/><text x="30.1509%" y="143.50">progset1`progset1::graph_gen::CompleteUnitGraph::graph_nd</text></g><g><title>progset1`hashbrown::map::HashMap&lt;K,V,S,A&gt;::insert (14 samples, 0.05%)</title><rect x="66.5089%" y="117" width="0.0480%" height="15" fill="rgb(214,148,48)" fg:x="19396" fg:w="14"/><text x="66.7589%" y="127.50"></text></g><g><title>progset1`hashbrown::raw::RawTable&lt;T,A&gt;::reserve_rehash (11 samples, 0.04%)</title><rect x="66.5192%" y="101" width="0.0377%" height="15" fill="rgb(232,36,49)" fg:x="19399" fg:w="11"/><text x="66.7692%" y="111.50"></text></g><g><title>progset1`core::hash::BuildHasher::hash_one (5 samples, 0.02%)</title><rect x="99.4239%" y="117" width="0.0171%" height="15" fill="rgb(209,103,24)" fg:x="28995" fg:w="5"/><text x="99.6739%" y="127.50"></text></g><g><title>progset1`core::hash::BuildHasher::hash_one (4 samples, 0.01%)</title><rect x="99.4445%" y="101" width="0.0137%" height="15" fill="rgb(229,88,8)" fg:x="29001" fg:w="4"/><text x="99.6945%" y="111.50"></text></g><g><title>progset1`hashbrown::map::HashMap&lt;K,V,S,A&gt;::get_mut (32 samples, 0.11%)</title><rect x="99.4445%" y="117" width="0.1097%" height="15" fill="rgb(213,181,19)" fg:x="29001" fg:w="32"/><text x="99.6945%" y="127.50"></text></g><g><title>progset1`core::hash::Hasher::write_i64 (28 samples, 0.10%)</title><rect x="99.4582%" y="101" width="0.0960%" height="15" fill="rgb(254,191,54)" fg:x="29005" fg:w="28"/><text x="99.7082%" y="111.50"></text></g><g><title>progset1`core::hash::BuildHasher::hash_one (9 samples, 0.03%)</title><rect x="99.5577%" y="101" width="0.0309%" height="15" fill="rgb(241,83,37)" fg:x="29034" fg:w="9"/><text x="99.8077%" y="111.50"></text></g><g><title>progset1`core::hash::Hasher::write_i64 (18 samples, 0.06%)</title><rect x="99.5885%" y="101" width="0.0617%" height="15" fill="rgb(233,36,39)" fg:x="29043" fg:w="18"/><text x="99.8385%" y="111.50"></text></g><g><title>progset1`hashbrown::map::HashMap&lt;K,V,S,A&gt;::insert (31 samples, 0.11%)</title><rect x="99.5542%" y="117" width="0.1063%" height="15" fill="rgb(226,3,54)" fg:x="29033" fg:w="31"/><text x="99.8042%" y="127.50"></text></g><g><title>progset1`hashbrown::raw::RawTable&lt;T,A&gt;::reserve_rehash (3 samples, 0.01%)</title><rect x="99.6502%" y="101" width="0.0103%" height="15" fill="rgb(245,192,40)" fg:x="29061" fg:w="3"/><text x="99.9002%" y="111.50"></text></g><g><title>progset1`progset1::prim_heap::PrimHeap&lt;T,W&gt;::heapify (4 samples, 0.01%)</title><rect x="99.6674%" y="117" width="0.0137%" height="15" fill="rgb(238,167,29)" fg:x="29066" fg:w="4"/><text x="99.9174%" y="127.50"></text></g><g><title>progset1`core::hash::BuildHasher::hash_one (4 samples, 0.01%)</title><rect x="99.6982%" y="85" width="0.0137%" height="15" fill="rgb(232,182,51)" fg:x="29075" fg:w="4"/><text x="99.9482%" y="95.50"></text></g><g><title>progset1`hashbrown::map::HashMap&lt;K,V,S,A&gt;::get_mut (5 samples, 0.02%)</title><rect x="99.7120%" y="85" width="0.0171%" height="15" fill="rgb(231,60,39)" fg:x="29079" fg:w="5"/><text x="99.9620%" y="95.50"></text></g><g><title>progset1`core::hash::BuildHasher::hash_one (4 samples, 0.01%)</title><rect x="99.7463%" y="69" width="0.0137%" height="15" fill="rgb(208,69,12)" fg:x="29089" fg:w="4"/><text x="99.9963%" y="79.50"></text></g><g><title>progset1`progset1::prim_heap::TwoWayVec&lt;T,W&gt;::get_weight_of_ind (33 samples, 0.11%)</title><rect x="99.7291%" y="85" width="0.1132%" height="15" fill="rgb(235,93,37)" fg:x="29084" fg:w="33"/><text x="99.9791%" y="95.50"></text></g><g><title>progset1`core::hash::Hasher::write_i64 (24 samples, 0.08%)</title><rect x="99.7600%" y="69" width="0.0823%" height="15" fill="rgb(213,116,39)" fg:x="29093" fg:w="24"/><text x="100.0100%" y="79.50"></text></g><g><title>progset1`core::hash::BuildHasher::hash_one (4 samples, 0.01%)</title><rect x="99.8628%" y="53" width="0.0137%" height="15" fill="rgb(222,207,29)" fg:x="29123" fg:w="4"/><text x="100.1128%" y="63.50"></text></g><g><title>progset1`progset1::prim_heap::PrimHeap&lt;T,W&gt;::bubble_down (66 samples, 0.23%)</title><rect x="99.6845%" y="101" width="0.2263%" height="15" fill="rgb(206,96,30)" fg:x="29071" fg:w="66"/><text x="99.9345%" y="111.50"></text></g><g><title>progset1`progset1::prim_heap::TwoWayVec&lt;T,W&gt;::swap (20 samples, 0.07%)</title><rect x="99.8423%" y="85" width="0.0686%" height="15" fill="rgb(218,138,4)" fg:x="29117" fg:w="20"/><text x="100.0923%" y="95.50"></text></g><g><title>progset1`hashbrown::map::HashMap&lt;K,V,S,A&gt;::get_mut (16 samples, 0.05%)</title><rect x="99.8560%" y="69" width="0.0549%" height="15" fill="rgb(250,191,14)" fg:x="29121" fg:w="16"/><text x="100.1060%" y="79.50"></text></g><g><title>progset1`core::hash::Hasher::write_i64 (10 samples, 0.03%)</title><rect x="99.8766%" y="53" width="0.0343%" height="15" fill="rgb(239,60,40)" fg:x="29127" fg:w="10"/><text x="100.1266%" y="63.50"></text></g><g><title>progset1`progset1::prim_heap::PrimHeap&lt;T,W&gt;::take_min (68 samples, 0.23%)</title><rect x="99.6811%" y="117" width="0.2332%" height="15" fill="rgb(206,27,48)" fg:x="29070" fg:w="68"/><text x="99.9311%" y="127.50"></text></g><g><title>progset1`core::hash::BuildHasher::hash_one (4 samples, 0.01%)</title><rect x="99.9211%" y="101" width="0.0137%" height="15" fill="rgb(225,35,8)" fg:x="29140" fg:w="4"/><text x="100.1711%" y="111.50"></text></g><g><title>dyld`start (29,155 samples, 99.97%)</title><rect x="0.0000%" y="261" width="99.9726%" height="15" fill="rgb(250,213,24)" fg:x="0" fg:w="29155"/><text x="0.2500%" y="271.50">dyld`start</text></g><g><title>progset1`main (29,154 samples, 99.97%)</title><rect x="0.0034%" y="245" width="99.9691%" height="15" fill="rgb(247,123,22)" fg:x="1" fg:w="29154"/><text x="0.2534%" y="255.50">progset1`main</text></g><g><title>progset1`std::rt::lang_start (29,154 samples, 99.97%)</title><rect x="0.0034%" y="229" width="99.9691%" height="15" fill="rgb(231,138,38)" fg:x="1" fg:w="29154"/><text x="0.2534%" y="239.50">progset1`std::rt::lang_start</text></g><g><title>progset1`std::rt::lang_start_internal (29,154 samples, 99.97%)</title><rect x="0.0034%" y="213" width="99.9691%" height="15" fill="rgb(231,145,46)" fg:x="1" fg:w="29154"/><text x="0.2534%" y="223.50">progset1`std::rt::lang_start_internal</text></g><g><title>progset1`std::rt::lang_start::_{{closure}} (29,154 samples, 99.97%)</title><rect x="0.0034%" y="197" width="99.9691%" height="15" fill="rgb(251,118,11)" fg:x="1" fg:w="29154"/><text x="0.2534%" y="207.50">progset1`std::rt::lang_start::_{{closure}}</text></g><g><title>progset1`std::sys_common::backtrace::__rust_begin_short_backtrace (29,154 samples, 99.97%)</title><rect x="0.0034%" y="181" width="99.9691%" height="15" fill="rgb(217,147,25)" fg:x="1" fg:w="29154"/><text x="0.2534%" y="191.50">progset1`std::sys_common::backtrace::__rust_begin_short_backtrace</text></g><g><title>progset1`progset1::main (29,154 samples, 99.97%)</title><rect x="0.0034%" y="165" width="99.9691%" height="15" fill="rgb(247,81,37)" fg:x="1" fg:w="29154"/><text x="0.2534%" y="175.50">progset1`progset1::main</text></g><g><title>progset1`progset1::mst_average (29,154 samples, 99.97%)</title><rect x="0.0034%" y="149" width="99.9691%" height="15" fill="rgb(209,12,38)" fg:x="1" fg:w="29154"/><text x="0.2534%" y="159.50">progset1`progset1::mst_average</text></g><g><title>progset1`progset1::mst::Mst&lt;V&gt;::from_prim (9,745 samples, 33.42%)</title><rect x="66.5569%" y="133" width="33.4156%" height="15" fill="rgb(227,1,9)" fg:x="19410" fg:w="9745"/><text x="66.8069%" y="143.50">progset1`progset1::mst::Mst&lt;V&gt;::from_prim</text></g><g><title>progset1`progset1::prim_heap::TwoWayVec&lt;T,W&gt;::upsert_min (17 samples, 0.06%)</title><rect x="99.9143%" y="117" width="0.0583%" height="15" fill="rgb(248,47,43)" fg:x="29138" fg:w="17"/><text x="100.1643%" y="127.50"></text></g><g><title>progset1`hashbrown::map::HashMap&lt;K,V,S,A&gt;::get_mut (11 samples, 0.04%)</title><rect x="99.9348%" y="101" width="0.0377%" height="15" fill="rgb(221,10,30)" fg:x="29144" fg:w="11"/><text x="100.1848%" y="111.50"></text></g><g><title>progset1`core::hash::Hasher::write_i64 (9 samples, 0.03%)</title><rect x="99.9417%" y="85" width="0.0309%" height="15" fill="rgb(210,229,1)" fg:x="29146" fg:w="9"/><text x="100.1917%" y="95.50"></text></g><g><title>all (29,163 samples, 100%)</title><rect x="0.0000%" y="277" width="100.0000%" height="15" fill="rgb(222,148,37)" fg:x="0" fg:w="29163"/><text x="0.2500%" y="287.50"></text></g><g><title>libsystem_kernel.dylib`__exit (8 samples, 0.03%)</title><rect x="99.9726%" y="261" width="0.0274%" height="15" fill="rgb(234,67,33)" fg:x="29155" fg:w="8"/><text x="100.2226%" y="271.50"></text></g></svg></svg>